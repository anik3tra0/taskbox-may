<h3>Total Categories: <span id="count"><%= @categories.size %></span></h3>

<ul id="listing">
	<% for category in @categories %>
		<%= render 'list', partial_category: category %>
	<% end %>
</ul>

<%= link_to "Add Category", new_category_path, remote: true, id: "add_category" %> 
<br/><br/>